# Build-only dependencies (release plan items 1.1 to 1.3).
# Never installed by end users; the build machine uses this file.
# Install with: pip install -r requirements-build.txt

-r requirements.txt
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
pillow==12.3.0
pefile==2024.8.26
altgraph==0.17.5
pywin32-ctypes==0.2.3
setuptools==84.0.0
